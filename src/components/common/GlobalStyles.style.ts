import { createGlobalStyle } from "styled-components";

export const GloablStyles = createGlobalStyle`
    html {
        min-height: 100%;
    }

    body {
        min-height: 100%;
        padding: 48px 0;
        margin: 0;
        font-family: arial;
    }
`;
